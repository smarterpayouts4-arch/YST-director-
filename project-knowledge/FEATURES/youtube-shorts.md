# FEATURE — YouTube Shorts

Status: **Partial** (P1A frozen + P1B storyboard + P1C production expansion)  
Owner: product

## Purpose

Channel generator for YouTube Shorts. Consumes a Canonical Topic Packet (Atom) into an immutable Shorts-owned portfolio, then storyboard → production expansion into seven complete scene packages for external still → voice → motion. This product does not render video or invent media.

Social Media is the **organizational parent only** (nav/journey hub). Shorts is a complete independent brain under that parent. Future Long-form / LinkedIn are sibling trees — never import Shorts creative internals.

## Partial now (P1A + P1B + P1C)

- Domain at `src/features/social-media/youtube-shorts/{brain,contracts,schemas,state,components,config,prompts,services,export}/`
- Routes: `/social-media` (nav hub only) → click YouTube Shorts → `/social-media/youtube-shorts`
- Ready Atom CTA **Send to Social Media** → IDs only (no ingest) → hub → channel click → **Shorts shell sole ingest owner**
- Hub must not import Shorts contracts/state or call ingest/persist
- Atom identity agreement lives at `content-intelligence/contracts/resolve-atom-identity.ts` (Shorts re-exports as `resolveShortsIdentity`)
- Storage key `social-media-youtube-shorts:v1` with `sessionsByTopicPacketId` multi-portfolio map
- Full immutable `ingestedAtom` snapshot; byte-stable re-ingest (no timestamp bump)
- 16-field projection contract (UI shows compact **Atom received** — not a field dump)
- Resume: Shorts storage first; one-time TE seed via `loadTopicSession` only when missing + `artifactId`
- P1B: `YOUTUBE_SHORTS_MODEL` / `getYouTubeShortsModel()` (default `gpt-5.6-terra`); one `generate-shorts-storyboard` operation; Shorts Story Brain at `brain/` (distilled MD → headed compile `prompts/story-brain/`; facade `prompts/story-brain.ts`; never RAG / never import `Reference/`); model locks `storyArchitecture` (`hookWhy`, `beats[].because`) then writes exactly 7 scenes; spoken first-hearing language + Hook procedure + causal beats + 22-word narration budget + spoken rewrite pairs + concept naming / two-beat payoff + closed-world locations / spoken-only analyst idioms / payoff compression + Scene Description Hook-withhold / anti-proxy bind + scene realization (strongest visible story event; story event first, not the information; screen only when it is the story event; robustness — as many sentences as the moment needs, typically four to seven, 200–1200 chars on the model contract covering committed place / people / subjects in concrete form / mood as felt atmosphere / withhold; a one-line caption is a defect; a repeat/padding sentence is also a defect — density, never padding; label info becomes a person's action; generic reserved for unnamed people) + Role contract (short name — one-sentence definition of the story function in THIS story, 40–240 chars) + Purpose contract (two to four dense sentences naming the viewer-change and the evidence this beat has earned, 100–700 chars) + per-scene compact semantic `situationLock` (`eventMode` physical_comparison | mediated_surface | dual required on the model lock, optional on persist; requiredSubjects / visibleActionOrState / relationship; locationConstraint defaults open; humanPresenceConstraint is exclusive floor/ceiling (none / hands / partial / person / open — hands is not an upgrade of open; partial is not a synonym of person); a deciding or comparing person goes on requiredSubjects with person or partial; Scene Description verbalizes the locked visibility amount; open marks staged place as P1B's free non-claim choice; not a shot card; no viewerMustPerceive / mustWithhold / physicalAction; online in audience is spoken truth not a device cue; panel-pointing only when eventMode is mediated_surface or dual) + construction order Role → Purpose → situationLock → Scene Description → Narration → OST with anti-caption (`ci-shorts-1.6.6`); validator: repeated-bigram echo check; generated/working/approved snapshots; owner review/edit/approve. No AI on mount. No Gemini writer. No third LLM hop.
- P1B workspace (density): 3-column status card (Draft Editable / Draft Edited / Approved Frozen); seven small 9:16 contact-sheet thumbs; field-tab scene editor; Story Map overlay with owner Edit Story for Role / Purpose / Narration on `workingStoryboard`. No image attachment.
- Scene package placement: selected scene uses a **field-tab inspector** — Role, Purpose, Scene Description, Timing, Narration, On-Screen Text (storyboard) plus Visual Prompt, Voice Direction, Asset Type, Motion Prompt, Continuity (production). One active panel; Copy ×3 enabled after expand.
- P1C: sibling `youtube-shorts-production` schema (`projectVisualContinuity`, optional CHARACTER\*, per-scene `continuityDelta` + visual/motion/voice/asset). One `expand-shorts-production` whole-board call (`ci-shorts-production-1.4.5`; headed compile `prompts/production-brain/`; human-crop integrity — `humanPresenceConstraint` is exclusive floor and ceiling (none = no human; hands = hands+forearms only; partial = identifiable person with face + connected upper-body context; person = primary subject, face and substantial upper body cannot be cropped away; open = decide from Scene Description, do not invent a person if SD stages none); POV / anonymous hands satisfy hands only and are illegal on partial/person; four-level priority: locked invariants → required relationship → production realization → Senior Director polish (may improve #3 only); Atom-named form is WHAT, unnamed form is HOW chosen once in projectVisualContinuity and held for the Short; anatomical ownership scales to that presence lock (hands plate = one limb, single named entry direction, cut line stated at wrist or forearm; partial/person = continuous shoulder-to-wrist); whenever a torso is visible the head is decided explicitly — whole head in frame with stated space above, or cut at a named line below the collarbone, and a face sliced by the top edge is a rendering failure not a crop; crop naming is unconditional for any human matter and protects face/crown alongside ankle/wrist/product cap; full-bleed plate — complete photograph from the top edge to the bottom edge, no unused painted space, no blank band / flat panel / empty header; visualPrompt never contains reservation, empty-area, safe-zone, or upper-third instructions; on-screen text is overlay at export and does not own any part of the still; production persona, not storyboard persona; situation fidelity before portrait craft; `eventMode` is WHAT — physical_comparison forbids laptop/phone/listing UI; mediated_surface may realize the named surface; dual allows both; unbranded hero products = no logo not cheap packaging; every plate names hero material/finish explicitly; hero labels are realistic generic unread structure — no readable letters, numbers, identity marks, or brands anywhere in the plate (A/B identity letters removed in 1.4.3); empty white labels are a defect; alternatives distinguished by their own forms and finishes, never painted-on characters; a story about price never renders a readable price; same-call senior-director pass — silently red-team the draft plate for default expression / stock posture / unconsidered wardrobe / beige-room staging, rewrite weak HOW, emit only the final plate; anti-genericity qualified — a plate serving a hundred unrelated stories is fixed by sharper direction from locked Purpose / relationship / action / visual world, never new props / places / characters; anatomical ownership — an acting limb must belong to its character via shoulder-elbow-wrist continuity, no off-camera/POV limb unless the lock stages another person; non-claim casting for the beat — wardrobe as situation-appropriate dress not a beauty/youth/luxury default, no sensitive-identity inference, never sexualize by default; deliberate environment register — ordinary/intimate/technical/institutional/documentary/aspirational/premium chosen from Purpose, premium never the default, undirected beige room equally a defect); approved storyboard is read-only context (storyArchitecture + Role / Purpose / situationLock / Scene Description / Timing / Narration / OST); P1C may read the locked story package and must never rewrite, return, mutate, summarize, or substitute those fields; story-to-plate doctrine (situationLock is WHAT; Scene Description's place/people/subjects/action are binding staging — P1C may not relocate, re-stage, or swap subjects; P1C resolves only what the lock and Scene Description leave unstated then writes visualPrompt in Subject → Action → Context → Composition → Visual treatment order; story-essential objects preserved; non-binding shortcuts = camera/packshot/lighting cliches only; heard-not-drawn; purpose→emphasis; one visual idea; self-contained plate; allowed neutral staging vs forbidden factual invention; native 9:16 portrait frame; visual finish — lighting / palette / depth / optics / materials; no unauthorized mediating surface; relationship-aware centre of interest; embodiment-over-explanation; lived-in staging; mood-driven finish; per-beat finish variation inside one world; gesture/crop craft; implied-information craft on already-present surfaces; relationship distinction; designed lower interaction zone; lightest useful human involvement when presence is open; category-credible hero-product finish; continuity inherits Scene 1 modality); merged on export from approved storyboard. Persist `generatedProduction` + `workingProduction` + `productionPromptVersion` + `productionGeneratedAt`. No `approvedProduction`. Clear production on storyboard regenerate/edit/reopen. Deterministic paste export in `export/format-scene-paste.ts`. No in-app image/Veo client.
- Continuity is an **internal** production concept — export maps only supported locks into `CHARACTER NAME` / `CHARACTER IDENTITY` / `CHARACTER CONTINUITY` when talent exists; never bare `CONTINUITY:` or `PROJECT VISUAL CONTINUITY` paste headers.
- Future scene-level repair (not implemented): revising Scene N must see Atom truth, the approved storyboard, neighbors N-1/N+1, and overall progression. No per-scene LLM in P1C v1.
- Reference HOW library on disk at `Reference/channels/youtube-shorts/` — **never** runtime-imported; **never** stored in session. No feature-local raw Reference corpus. Shorts-owned brain MD under `src/features/social-media/youtube-shorts/brain/` is a deliberate distill, not a raw dump. Working director files: Arijon visual grammar + Milne scene direction (P1C). Raw books retired from the active repo (provenance in SOURCE_MAP).

## Not shipped yet

- External stills into 9:16 frames / continuity still review / motion generation UI
- Sibling channels (`youtube-long-form/`, etc.)

## External still paste target (not in-app)

When pasting a `visualPrompt` into Gemini, use an **image** model at **9:16**. Prefer `gemini-3.1-flash-image`. `gemini-2.5-flash-image` remains valid. Never use 3.6 Flash / Flash-Lite / Flash Cyber to generate the still. Veo is motion only. This product does not call Gemini or Veo.

## Non-goals (current phase)

RAG, runtime Reference imports, shared Social Media creative brain (`SOCIAL_MEDIA_MODEL`, parent `prompts|state|schemas|services`), lifting ingest to parent `social-media/` beyond nav, Social Media Atom store, reopening TE/Librarian freezes, Gemini writer swap, third LLM hop, in-app image/Veo client, promoting BeatStateSchema shot-card fields (`actor` / `physicalAction` / required `location`) onto P1B.
