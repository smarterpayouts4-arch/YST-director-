/**
 * Executable model instruction bundle version stamped into runtime prompt builders
 * and AI traces. Distinct from exported product contract `PROMPT_VERSION` (1.1.0).
 */
export const RUNTIME_PROMPT_VERSION = "rpb-runtime-1.4.0";
