/**
 * Compact Shorts-owned production expansion doctrine (P1C).
 * Distilled principles only — never runtime-import Reference/.
 * Arijon / Milne attribution is engineering provenance, not model input files.
 * Runtime compile: ./production-brain/ (headed modules; this file is the facade).
 */
export {
  SHORTS_PRODUCTION_EXPANSION_DOCTRINE,
  PRODUCTION_BRAIN_MODULES,
  PRODUCTION_BRAIN_SECTION_ORDER,
  compileProductionBrain,
} from "./production-brain/compile-production-brain";
