# Context selection

1. Open only the selected workflows' `required_context` stubs under `project-context/`.
2. Treat stubs as **pointers** — read the linked `project-knowledge/` document.
3. Prefer RPB MCP (`rpb_read_project_doc`) when available.
4. Do not load MarketMonth archive doctrine as live truth.
